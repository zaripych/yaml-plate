{
  "name": "yaml-plate",
  "version": "0.0.1",
  "description": "Simple YAML templating using JavaScript",
  "main": "index.js",
  "author": "@zaripych",
  "license": "MIT",
  "scripts": {
    "build": "node ./scripts/build.js",
    "declarations": "tsc -p ./tsconfig.declarations.json",
    "prebuild": "node ./scripts/clean.js",
    "clean": "node ./scripts/clean.js",
    "test": "jest --color",
    "integration": "jest --config ./jest.config.integration.js --color -i",
    "combine-coverage": "node ./scripts/combineCoverage.js",
    "type-check": "tsc",
    "lint": "tslint -p . -t verbose",
    "check": "concurrently \"node ./scripts/build.js\" \"tsc\" \"tslint -p . -t verbose\"",
    "patch": "ts-deps patch",
    "release": "semantic-release"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS",
      "pre-commit": "pretty-quick --staged",
      "pre-push": "npm run check"
    }
  },
  "dependencies": {
    "@types/yargs": "13.0.0",
    "fast-glob": "2.2.6",
    "fs-extra": "7.0.1",
    "js-yaml": "3.13.1",
    "rxjs": "6.4.0",
    "rxjs-marbles": "5.0.0",
    "rxjs-stream": "3.0.1",
    "yargs": "13.2.2"
  },
  "devDependencies": {
    "@types/js-yaml": "3.12.1",
    "@types/node": "8.10.40",
    "ts-deps": "0.3.1"
  }
}
